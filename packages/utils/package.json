{
  "name": "@chatowa/utils",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": { "date-fns": "^3.6.0", "zod": "^3.23.0" },
  "devDependencies": { "typescript": "^5.4.0", "vitest": "^4.1.7" }
}